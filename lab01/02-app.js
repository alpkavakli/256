// nodejs module system: ESM

import { add, area } from "./02-math.js" ; 

const radius = 7;
console.log(`Sum of 2 and 3 is ${add(2,3)}`);
console.log(`Area of circle of radius ${radius} is ${area(radius)}`);   